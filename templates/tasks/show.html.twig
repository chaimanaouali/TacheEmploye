<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Details</title>
    <!-- Bootstrap CSS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        .table th, .table td {
            text-align: left;
        }
        .btn-back {
            margin-right: 10px;
        }
    </style>
</head>
<body>
    <div class="container my-4">
        <h1 class="mb-4">Task Details</h1>

        <table class="table table-bordered">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ task.id }}</td>
                </tr>
                <tr>
                    <th>Task</th>
                    <td>{{ task.task }}</td>
                </tr>
                <tr>
                    <th>DateR</th>
                    <td>{{ task.dateR ? task.dateR|date('Y-m-d') : 'N/A' }}</td>
                </tr>
                <tr>
                    <th>Etat</th>
                    <td>{{ task.etat }}</td>
                </tr>
                <tr>
                    <th>TypeT</th>
                    <td>{{ task.typeT }}</td>
                </tr>

                 <tr>
            <th>Created At</th>
            <td>{{ task.createdAt ? task.createdAt|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
        <tr>
            <th>Updated At</th>
            <td>{{ task.updatedAt ? task.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
            </tbody>
        </table>

        <div class="mb-3">
            <a href="{{ path('app_tasks_index') }}" class="btn btn-secondary btn-back">Back to List</a>
            <a href="{{ path('app_tasks_edit', {'id': task.id}) }}" class="btn btn-primary">Edit</a>
        </div>

        {{ include('tasks/_delete_form.html.twig') }}
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
